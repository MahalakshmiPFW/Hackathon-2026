# NLP service dependencies
# Pin major versions — BERT inference is sensitive to transformers version mismatches

fastapi>=0.111.0,<0.200.0
uvicorn[standard]>=0.29.0,<0.30.0       # ASGI server with websocket support
pydantic>=2.7.0,<3.0.0                  # request/response validation

torch>=2.2.0                             # CPU build by default; for GPU: torch>=2.2.0+cu121
transformers>=4.40.0,<5.0.0             # must match version used in Colab
safetensors>=0.4.0                      # loads model.safetensors checkpoint

# Optional but recommended
python-multipart>=0.0.9                 # needed if you add file upload routes later
httpx>=0.27.0                           # for test_client in tests
pytest>=8.0.0                           # test runner